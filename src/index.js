import { Queue } from "./data-structures/queue";

// Queues

const queue = new Queue();

queue.add("first");
queue.add("last");
queue.add("last +");
queue.remove() //first removed
console.log(queue);
